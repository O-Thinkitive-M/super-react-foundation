// Typed path constants + builders — the single source of truth for URLs. No
// magic route strings in components; build links from here. See project-setup/routing.md.
export const paths = {
  home: "/",
  dashboard: "/dashboard",
} as const;
