# utils/

Small, **pure, framework-agnostic** helper functions (formatting, dates, guards). No React, no app state. Heavier reusable logic (data structures / algorithms) lives in `lib/ds` and `lib/algo`.
