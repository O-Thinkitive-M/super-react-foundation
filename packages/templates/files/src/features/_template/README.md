# features/_template/

Canonical feature skeleton. **Copy this folder** to `features/<your-feature>/` to start a feature, then rename and fill in. Delete `_template/` if you do not want the example skeleton.

Structure: `api/`, `components/`, `pages/`, `hooks/`, `config/`, `index.ts` (public barrel).
