# features/<feature>/api/

Feature-specific query/mutation wrappers over the generated `sdk/`. Wrap raw SDK calls in React Query hooks (`useXxxQuery`, `useXxxMutation`) with query keys + cache invalidation.
