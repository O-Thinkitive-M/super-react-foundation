# sdk/

**Generated API client (Orval / OpenAPI). Do not hand-edit** — it is overwritten on regeneration. Configure generation to use the axios mutator in `api/`. Wrap SDK calls in React Query hooks under each feature's `api/` folder.
