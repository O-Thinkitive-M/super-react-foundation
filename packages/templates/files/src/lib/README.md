# lib/

Pure, framework-agnostic, reusable building blocks. **No React, no app state** — must stay portable.

| Folder | Purpose |
| --- | --- |
| `ds/` | Reusable data structures (collections, LRU, normalize, tree). |
| `algo/` | Pure algorithms (binary search, comparators, graph, intervals, rate). |

See `project-setup/data-structures.md`.
