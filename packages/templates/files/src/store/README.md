# store/

Global **client state** stores. Server/remote state belongs in React Query, not here. See `project-setup/state-management.md` for the chosen library (Redux Toolkit `*.slice.ts` or Zustand `*.store.ts`) and what belongs in client state.
